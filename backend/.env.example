# Conference Games Backend Environment Configuration

# Server Configuration
NODE_ENV=development
PORT=3001
LOG_LEVEL=info
CORS_ORIGIN=*

# Database Configuration (Optional)
# DATABASE_URL=postgresql://username:password@localhost:5432/conference_games
DB_MAX_CONNECTIONS=20
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=2000

# Timer Configuration
TIMER_DEFAULT_SECONDS=30
HISTORY_SIZE=50

# Content Configuration
CONFIGMAP_PATH=/etc/config
CONTENT_CACHE_TIMEOUT=300000

# Development/Testing
# Set to true to enable file watching for ConfigMap changes
# WATCH_CONFIGMAPS=true

# Production Settings
# In production, these should be set via Kubernetes secrets/configmaps:
# - DATABASE_URL (if using database)
# - CONFIGMAP_PATH (path to mounted ConfigMap volume)
# - CORS_ORIGIN (specific origins for security)

# Timer Duration Options (comma-separated seconds)
# These can be overridden via the configuration API at runtime
# Default: 15,30,45,60
# TIMER_AVAILABLE_OPTIONS=15,30,45,60

# Content Loading
# Path where sentences.json and acronyms.json are mounted
# In Kubernetes, this would be a ConfigMap volume mount
# CONFIGMAP_PATH=/etc/config

# Cache timeout for content in milliseconds
# How long to cache content before checking for updates
# CONTENT_CACHE_TIMEOUT=300000

# Database Connection Pool Settings
# Maximum number of database connections in the pool
# DB_MAX_CONNECTIONS=20

# How long a connection can be idle before being closed (ms)
# DB_IDLE_TIMEOUT=30000

# How long to wait when connecting to database (ms)
# DB_CONNECTION_TIMEOUT=2000

# Security Settings
# CORS origins - use specific domains in production
# CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com

# Logging
# Available levels: error, warn, info, debug
# LOG_LEVEL=info